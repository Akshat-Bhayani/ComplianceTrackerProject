import React from 'react';

const PositionSetup = () => (
    <div>PositionSetup Module</div>
);

export default {
    routeProps: {
        path: '/position-setup',
        component: PositionSetup
    },
    name: 'PositionSetup',
}
