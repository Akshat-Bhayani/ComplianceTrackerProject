import React from 'react';

const SideBar = () => (
    <div>SideBar Module</div>
);

export default {
    routeProps: {
        path: '/side-bar',
        component: SideBar
    },
    name: 'SideBar',
}
