import React from 'react';

const FunctionSetup = () => (
    <div>FunctionSetup Module</div>
);

export default {
    routeProps: {
        path: '/functionsetup',
        component: FunctionSetup
    },
    name: 'FunctionSetup',
}
